import type { Metadata } from "next";
import "./globals.css"; // Ensure this file exists in src/app/

export const metadata: Metadata = {
  title: "AI Academic Result Intelligence & Ranking System",
  description: "Transform academic results into actionable intelligence with AI-powered OCR, structured analytics, and leaderboards.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
        <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
      </head>
      <body>{children}</body>
    </html>
  );
}